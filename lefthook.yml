pre-commit:
  parallel: true
  commands:
    lint:
      run: npm run lint
    format:
      run: npx prettier --check "src/**/*.ts" "test/**/*.ts"
    typecheck:
      run: npx tsc --noEmit

pre-push:
  commands:
    typecheck:
      run: npx tsc --noEmit
    build:
      run: npm run build
